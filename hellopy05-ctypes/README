
require Scons only (of course, Python-2.7 is required^^), 
install them before trying the following staff

-------------------------------------------------------

run the following command in console
>> scons 
to generate the shared library `libtest.so'

then, run the following in console
>> python test.py
to get the final result^^